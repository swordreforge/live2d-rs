[workspace]
resolver = "2"
members = [
    "live2d-core-sys",
    "live2d-core",
    "live2d-viewer",
]

[profile.release]
strip = true
lto = true
panic = "abort"
overflow-checks = false
debug = false
debug-assertions = false
incremental = false
